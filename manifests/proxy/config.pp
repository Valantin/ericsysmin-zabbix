# == Class: zabbix::proxy::config
# This class configures the zabbix proxy configuration by combining the template and the configuration values from zabbix::proxy::params.
#
# === Sample Usage:
# class {'zabbix::proxy::config': } or include zabbix::proxy::config
#
